import { AllExceptionsFilterExample } from './all-exceptions-filter.filter';

describe('AllExceptionsFilterFilter', () => {
  it('should be defined', () => {
    expect(new AllExceptionsFilterExample()).toBeDefined();
  });
});
