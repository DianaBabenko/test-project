import { Injectable } from '@nestjs/common';

@Injectable()
export class CatsRepository {
  // constructor(@Inject('CONNECTION') connection: Connection) {}
  // constructor(@Inject('ASYNC_CONNECTION') asyncConnection) {}
}
